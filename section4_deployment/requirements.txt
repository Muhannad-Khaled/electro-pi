# Direct dependencies, pinned. llama-cpp-python installs from the prebuilt
# CPU wheel index (see Dockerfile / README) to avoid a native compile.
fastapi==0.139.2
uvicorn[standard]==0.51.0
llama-cpp-python==0.3.34
httpx==0.28.1
huggingface_hub==1.24.0
